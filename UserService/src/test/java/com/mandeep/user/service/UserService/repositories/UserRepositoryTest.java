package com.mandeep.user.service.UserService.repositories;
//import com.demo.entites.Person;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.boot.test.autoconfigure.orm.jpa.DataJpaTest;

import com.mandeep.user.service.UserService.entities.User;

import static org.assertj.core.api.Assertions.assertThat;

/*@org.springframework.boot.data.jpa.test.autoconfigure.DataJpaTest
public class UserRepositoryTest {
	@Autowired
    private User userRepo;

    @Test
    public void testExistsById() {
        User user = new User(null,"myName", "Email", "City", null);
        user = ((Object) userRepo).save(user);
        boolean exists = userRepo.existsById(user.getUserId());
        assertThat(exists).isTrue();
    }

}*/
